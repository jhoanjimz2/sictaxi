import { FechasVencidasPipe } from './fechas-vencidas.pipe';

describe('FechasVencidasPipe', () => {
  it('create an instance', () => {
    const pipe = new FechasVencidasPipe();
    expect(pipe).toBeTruthy();
  });
});
